# ─────────────────────────────────────────────────────────
# k8s-lifecycle-manager — Python Dependencies
#
# Install with:
#   pip install -r requirements.txt    (Windows)
#   pip3 install -r requirements.txt   (Linux / Mac)
# ─────────────────────────────────────────────────────────

# Fetches latest stable Kubernetes version from internet
requests>=2.28.0

# Reads config/settings.yaml
pyyaml>=6.0

# Sends commands to remote server via SSH
ansible>=8.0.0

# Pure Python SSH library — used for password-based auth
# Works on Windows without needing sshpass or any extra tools
paramiko>=3.0.0

# ─────────────────────────────────────────────────────────
# NOT REQUIRED on your laptop:
#   kubectl  → runs on the remote server
#   kubeadm  → runs on the remote server
#   kubelet  → runs on the remote server
# ─────────────────────────────────────────────────────────
